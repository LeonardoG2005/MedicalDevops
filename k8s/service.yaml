apiVersion: v1
kind: Service
metadata:
  name: medical-documents-api
  namespace: default

spec:
  type: ClusterIP

  selector:
    app: medical-documents-api

  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP